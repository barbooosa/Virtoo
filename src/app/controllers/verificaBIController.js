 /////////////////////////////////// Verifica se o BI é válido
 function verificaBIController(angolaBI) {
    console.log('FSDF')
    //     // Remover espaços em branco e converter para maiúsculas
//     angolaBI = angolaBI.replace(/\s/g, '').toUpperCase();

//     // Verificar se o BI tem o formato correto (9 dígitos)
//     if (!/^\d{9}$/.test(angolaBI)) {
//         return false;
//     }

//     // Lógica para verificar se o BI é válido
//     // (Você pode implementar a lógica de verificação aqui)

//     // Por enquanto, retornamos true para todos os BIs
//     return true;
// }


};

   

module.exports = new verificaBIController();